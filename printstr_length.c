#include "main.h"

/**
 * _strlen - print length of a string
 * @str: string pointer
 * Return: i
 */

int _strlen(char *str)
{

	int i;
	for (i = 0; str[i] != 0; i++)
	return (i);


}

/**
 * _strlenchar - Const Character string lenth
 * @str: character pointer
 * Return : 1 success
 */

int _strlenchar(const char * str)
{

	int i;

	for (i = 0; str[i] != 0; i++ )
	return (i);

}

